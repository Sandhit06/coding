#include <stdio.h>
#include <stdlib.h>
int top=-1,n;
scanf("%d", &n);
int arr[n];
int main()
{
    void push();
    void pop();
    void show();
    int main()
    {
        int ch;
        scanf("%d", &ch);
        switch(ch)
        {
            case 1: Push();
                break;
            case 2: Pop();
                break;
            case 3: show();
                break;
            case 4: exit(0);

            default:
            printf("\n Invalid Choice ");
        }
    }
}
void push()
{
    int x;
    if(Top==n-1)
    {
        printf("")
    }
}